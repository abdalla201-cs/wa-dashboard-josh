/* ============================================================
   LEAD FILTER  —  SUBSCRIPTION CONTROL  (Josh Cohen)

   This is the ONLY file you edit to manage the subscription.

   ▸ TO RENEW / EXTEND:  change "expires" to a later date, then Commit.
   ▸ TO CANCEL / LOCK:   set "expires" to a past date  -> tool locks instantly.
   ▸ Date format: "YYYY-MM-DD"  (year-month-day).

   "hash" is the access key's fingerprint. Leave it alone unless you are
   rotating the key (your provider will give you a new hash + key).
   ============================================================ */
window.WAC_LICENSE = {
  expires: "2026-01-01",
  hash: "d8842e9ba23c983958e5bfee03492f74d9c1e890cb05fb44f7526087e81c16b6"
};
